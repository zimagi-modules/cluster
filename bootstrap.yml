- hosts: all
  roles:
    - { role: geerlingguy.docker }
    - { role: geerlingguy.ntp }

- hosts: "{{ groups['all'][0::2] }}"
  roles:
    - { role: cenv }

- hosts: "{{ groups['all'][1::2] }}"
  roles:
    - { role: cenv }

parents:
  - module: cluster
    profile: aws-network

config:
  admin_groups: cenv

group:
  cenv:

domain:
  "@root_domain":
    provider: route53
    email: "@root_domain_email"
    groups: "@admin_groups"

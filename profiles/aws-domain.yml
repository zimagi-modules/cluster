parents:
  - module: cluster
    profile: aws-network

config:
  dns_timeout: 300

  admin_groups: cenv

group:
  cenv:

domain:
  "@root_domain":
    provider: route53
    email: "@root_domain_email"
    groups: "@admin_groups"

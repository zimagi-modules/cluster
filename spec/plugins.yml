plugin_mixins:
    ssh_task:
        class: SSHTaskMixin
        requirement:
            servers:
                type: list
                help: "SSH server names"
        option:
            filter:
                type: str
                default: AND
                help: "Server filter conditional"

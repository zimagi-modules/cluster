command:
    server:
        rotate:
            base: server
            priority: 1
            parse:
                server_search: true

        ssh:
            base: server
            priority: 5
            parse:
                server_name: false

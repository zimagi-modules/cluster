command:
    firewall:
        resource: firewall
        base_name: firewall
        base: firewall
        priority: 78

        rule:
            resource: firewall_rule
            base_name: firewall_rule
            base: firewall
            priority: 1

command:
    network:
        resource: network
        base: network
        priority: 74

        peering:
            resource: network_peering
            base: network
            priority: 1

    subnet:
        resource: subnet
        base: subnet
        priority: 76

command:
    network:
        resource: network
        base_name: network
        base: network
        priority: 74

        peering:
            resource: network_peering
            base_name: network_peering
            base: network
            priority: 1

    subnet:
        resource: subnet
        base_name: subnet
        base: subnet
        priority: 76
